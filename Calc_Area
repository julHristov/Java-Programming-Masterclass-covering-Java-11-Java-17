//Coding exercise 8 from Udemy course Java Programming Masterclass covering Java 11 & Java 17
//The program calculates the area of a circle and rectangle, using overloading method;

public class Main {
    public static void main(String[] args) {
        System.out.println(area(-1.0, 4.0));
    }
    public static double area(double radius){
        if(radius < 0){
            return -1.0;
        }
        return Math.PI * (Math.pow(radius, 2));
    }
    public static double area(double x, double y){
        if(x < 0 || y < 0){
            return -1.0;
        }
        return x * y;
    }
}
